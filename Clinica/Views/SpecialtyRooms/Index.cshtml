@model IEnumerable<Clinica.Models.SpecialtyRoom>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.specialty)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.specialty)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idSpecialtyRoom">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idSpecialtyRoom">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idSpecialtyRoom">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
